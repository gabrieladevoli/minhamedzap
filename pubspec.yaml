name: minha_med_zap
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/medico1.gif
    - assets/medico2.gif
    - assets/medico3.gif
    - assets/logo.png
    - assets/borda.gif
    - assets/logo_empresa.png
# eu adicionei as imagens do logo e da borda, e também adicionei a imagem do médico para a home page assim pois não sabia como fazer.
